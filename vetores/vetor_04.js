// Maior e menor numero com a biblioteca Math

let idades = [1001, 8, 15, 25, 6, 4, 16, 90, 1]

   let maior = Math.max(...idades)
   let menor = Math.min(...idades)

    console.log(`o maior é ${maior} e menor é ${menor}`)

